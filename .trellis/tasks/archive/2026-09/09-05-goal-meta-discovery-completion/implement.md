# 执行计划
1. 获准后核对本机路径/链接/哈希与源的同步机制；结果写 research，不修改安装目录。
2. 核实具名 Linux 访问路径后只读对比审计中的两个路径。未取得时记录精确缺证据及受阻验收项 O2/O3，继续可完成的本地工作。
3. 比较非 Trellis 增量、完成条件、暂停规则及样例；列出具体处置选项。改变 Trellis 的方案排除。
4. 获得具体目标处置授权后再修改相应源/副本；现有 canonical 已满足要求时不为制造 diff 而重写。维护 O4/O5 行为 eval，保持同轮安全保存回归。
5. 运行 node --test skills/developer-tools-integrations/goal-meta-skill/tests/*.test.mjs；必要元数据变化运行 just docs-sync，再 just ci。
6. 在实际宿主新会话枚举发现入口、名称、版本、来源，按安装机制判断是否需重启。没有现场证据时 O3 不通过，不将本地测试替代。
7. 记录 O1—O6 证据，默认不提交/安装/发布；回滚使用处置前保留的精确副本/链接信息，不触碰无关目录。
