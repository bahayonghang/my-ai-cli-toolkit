# Implementation evidence

日期：2026-09-10。角色：trellis-implement；本子任务实施完成，独立 check 与父级最终 CI 待主线程执行。未提交、归档、激活 Goal 或保存用户合同。

## Changes and ownership

- R1：修订 interview 四处中英 draft/final 完成条件和字段一览，使交付存在、具名入口行为、必需检查、授权 diff/status 同时成立；缺检查/访问或超限未满足仍为 incomplete。原暂停类别保留。
- R2：default strategy、playbook、interview 和 Trellis 尾段均明确旧 nested path 只可聊天引用，不承诺写入或迁移。根目录 GOAL.md/alternate basename 使用既有 writer 的 --name，不把子目录充当 --repo-root。
- R3：root 负责选择平台与专项 reference、只读侦察、有限访谈、compile/lint/present、已授权保存/readback/stop。interface default_prompt 缩为意图、路由、权限边界；compatibility/trust/permissions/degradation 未删减。
- R4：升级 0.8.2；原 assertions schema 新增 eval 57–59；现有 creation-handoff、prior-art、enriched Skill IR 同步当前行为和证据；保留旧版本历史。category AGENTS 只纠正 goal-meta expectations 现状一句。
- 根据主线程批准，最小额外同步 persistent-goal-contract 的 Generated by 版本和 trellis-goal-cadence 末尾 legacy 段。linter 硬编码版本与 persist test fixture 的同步由原 lint implementer 独占完成，本角色未编辑 helper。
- docs-sync 只产生 docs/skills/developer-tools-integrations/goal-meta-skill.md 和 docs/en/skills/developer-tools-integrations/goal-meta-skill.md 的版本变更。
- 原子任务的 stdin/逐payload/长度行为和首行尾空白修复保留；用户 .trellis/.gitignore、已删除 reviews、其他任务及主线程 spec 编辑均未回退。

## Preserved capability matrix

| Capability | Reachable authority and evidence |
| --- | --- |
| Draft/approved text, compile-only, no activation | root Governed authority/S4–S6、interface permissions；review gate package test |
| Same-turn explicit save | root S6、persistent-goal-contract Trigger；eval 48/49/56 保留 |
| Create-only/hash/reparse/readback/non-Git snapshot | persistent-goal-contract；12 项 persistence tests 全通过 |
| Five renderers and management isolation | root S0/S6/platform-facts 链接；five platform lint/persistence tests |
| Ordinary conjunction | playbook canonical contract、interview 4 例；published examples test |
| Material-only bounded interview | root S2/S3、interview；不授予 AskUserQuestion 实际调用能力 |
| Scan envelope/stable ledger/independent feedback/rescan/no second Prompt | root S0 必选 reference；原 review profile 回归全部保留；新 eval59 是 recorded_fixture |
| Trellis default-on/opt-out/fallback/closeout | root S0 必选 cadence；测试从 interface 重复正文迁至 root 路由和权威 cadence，旧安全断言保留 |
| Provenance/evidence | 原 reports 分当前/历史，均未声称 provider/安装/人类效果 |

## Ordinary example review

完整普通可复制示例：playbook 双语 packet 2 例、Strong Examples 3 例、Anti-Patterns 的 Better 1 例、interview 4 例，共 10 例，均读取并通过 lint；前两组和 interview/Better 的完成语义经测试或人工逐项核对。Strong Examples 的 3 条 Claude 条件变体复用前置完整合同验证。持久 schema 四项完成门由原测试覆盖。

default strategy discovery-first 仍含任务占位符，是模板而非完整可复制 Goal；其完成语义人工核对为四项合取。其他 placeholder 模板、故意 Weak 反例、管理命令与短 launcher 不伪装成完整普通 Goal。

## Character metric

Python Path.read_text(encoding='utf-8')，统一 LF：

| Surface | Baseline | Current |
| --- | ---: | ---: |
| SKILL.md | 9803 | 6237 |
| agents/interface.yaml | 5246 | 2804 |
| Combined | 15049 | 9041 |

减少 6008 字符，39.92%。这是字符/重复负担度量，不是 token、provider 遵循率或语义能力提升证据。

## Checks

- PASS：最终 rtk node --test 两套目标 suite：73 tests，73 passed，0 failed，0 skipped（10305 ms）。覆盖第一子任务所有新增边界。
- PASS：just skills-check。
- PASS：just python-check，65 Python files。
- PASS：just docs-sync，已审阅生成 diff，仅两个 goal-meta version 条目。
- PASS：git diff --check。
- FIXED TEST ISSUE：补充普通示例断言时，将具名 local toolbar page 误限定为必须字面 entry point，导致一次测试失败；放宽测试至这两种真实具名入口后最终 73/73。未用修改产品行为迎合断言。
- RECORDED_FIXTURE：人工审阅 eval57–59 的 completion、nested-path、combined-reference 边界；仓库 CI 不执行这些 eval，不是 provider run。
- PENDING：独立 trellis-check/AC1–AC4、父级最终 just ci。PRD AC 保持未勾选。
- missing evidence：provider 前后对照、真实安装与新会话接力、人工盲评、线上平台事实刷新、遥测。此次不运行 raw exporter 覆盖 enriched IR，不做依赖/新 schema/README/manifest 扩展。
