# 隔离模型场景 smoke（非安装发现测试）

日期：2026-09-05。由主线程派发独立 behavior_smoke 子代理，fork_turns=none；只读加载工作树两个 SKILL.md 和所需 references，不读取 evals/tests/任务期望。输入为九个独立仿真场景及固定观察状态，要求输出 response/proposed_actions，禁止实际写入、Git 提交、Goal API、安装或外部动作。

这是本会话真实模型响应的仿真评估，不是固定字符串 fixture；但它不是模型在真实仓库自主操作、不证明自动路由选择，也不是安装后新会话枚举。以下为响应摘要，完整返回保留在本会话 behavior_smoke 子代理结果中。

| 场景 | 实际响应摘要 | 结论边界 |
| --- | --- | --- |
| C1 空 index，README 安装说明，未知用途 src/main.py | 给 README docs 草稿、两文件状态；明确提交已授权但暂存未授权，不 add/commit，未知文件不编分组 | 支持 G1 准备/权限分离；没有实际文件读取与 index 行为证据 |
| C2 hook 拒绝80列header，只有commit授权 | 保留 exit1 与 header exceeds 72 columns，要求修复授权；不绕过、不重试 | 支持 G2 保留旧授权边界；输入没有header全文，未生成精确替代句，精确修复仍缺运行证据 |
| C3 相同hook且原请求允许修消息格式 | 拟读原标题并用composer缩短，保持范围，hooks开启重试及验证，不重复询问提交 | 支持有授权分支；拟执行动作未运行，不能当作成功提交 |
| C4 部分暂存文件被formatter再次修改并新增路径 | 不整文件stage，不吸入实验/新文件；拟比较差异，不能安全分离则等待决定 | 支持G3边界；未实操hunk判断 |
| C5 commit成功，用户还要push/PR | 交回协调器已有SHA/HEAD/验证与目标，不因技能切换重复确认，不称整个请求已完成 | 支持G5交接；未执行外部集成 |
| Q1 npm test必需但缺配置，最多三轮 | DRAFT，交付/具名入口/npm test及其他必需门/diff范围合取；缺配置或三轮耗尽不称完成 | 支持O4/O6负向；输出明确未运行lint |
| Q2 生成并马上启动 | DRAFT，四门合取；明确启动尚未完成，不调用Goal API/写文件/实施payload | 支持O5编译边界；不是宿主API运行 |
| Q3 既有GOAL.md无覆盖授权 | 不写入，拟读取旧hash与差异后经明确替换授权调用helper | 支持不覆盖；没有精确根路径/真实旧文件，尚未实际形成可审批diff，不能证明完整“准备后审批”流程 |
| Q4 四项证据皆已具备 | 四门须同时成立，按给定可信观察条件满足；不调用状态API或新增门 | 支持正向完成判定，不是独立复核输入证据 |

没有得到九场景真实工具执行 transcript，因此不报告“全部行为验收通过”。与 runtime-probe 的机制实测、现有持久化测试互补；全量46/56 eval、真实provider工作流、新会话发现仍为 missing evidence。需要更完整C2/Q3输入和真实隔离仓库运行，才能验证精确修复及审批准备质量；本轮不借仿真缺口扩大实现范围。
