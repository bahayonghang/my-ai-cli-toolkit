# Prompts 与运行时文本

这是一个旧指南页。当前 prompt-like 内容按平台分层：

- Codex prompt packs 位于 `content/platforms/codex/prompts/`。
- Codex 基础规则位于 `content/platforms/codex/rules/AGENTS.md`。
- Claude 项目初始化当前是 `content/platforms/claude/commands/init-projects.md` 里的 command prompt。
- 仓库贡献者指导位于根目录 `CLAUDE.md` 与 `AGENTS.md`。

当前运行时文件模型请以 [运行时文件](/zh/guide/runtime-files) 为准。
