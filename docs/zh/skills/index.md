# 技能概览

本节文档对齐 `content/skills/` 下当前可安装的技能目录。下面的 live catalog 会直接链接到每个技能详情页，左侧侧边栏也会同步使用同一份目录数据。

<SkillsCatalog locale="zh" />

## 说明

- 文档目录只覆盖当前 `content/skills/` 中真实存在的可安装技能目录。
- 技能页关注“技能本身”和关键支撑资源，不会把技能内部每个 vendored reference 文件都镜像成单独页面。
- 文档目录只覆盖当前仍存在于 `content/skills/` 下的 live 可安装技能。
- 对当前论文工作流，应优先查看 `paper-workbench`；若干旧 academic skill 页面现在只保留为兼容 wrapper 说明。
