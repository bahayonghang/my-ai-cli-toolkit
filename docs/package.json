{
  "name": "myclaude-skills-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "catalog:sync": "node scripts/generate_skills_catalog.mjs",
    "dev": "npm run catalog:sync && vitepress dev --port 4000",
    "build": "npm run catalog:sync && vitepress build",
    "preview": "npm run catalog:sync && vitepress preview",
    "audit:sync": "npm run catalog:sync && node scripts/run-python.mjs scripts/audit_sync.py",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "npm run lint -- --fix"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "eslint": "^9.25.1",
    "vitepress": "2.0.0-alpha.17"
  }
}
