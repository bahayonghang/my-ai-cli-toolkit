interface:
  display_name: "Git Commit CN"
  short_description: "Safely plan, draft, or execute Chinese Conventional Commits for staged changes"
  default_prompt: "Use $git-commit-cn to inspect staged changes, decide whether they need to be split, and then draft or execute a safe Chinese Conventional Commit without pushing."
