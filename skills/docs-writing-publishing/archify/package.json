{
  "name": "archify",
  "version": "2.6.0",
  "private": true,
  "type": "module",
  "description": "JSON-IR diagram renderers (workflow / sequence / dataflow / lifecycle).",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node tests/golden.mjs && node --test tests/*.test.mjs",
    "render:examples": "node scripts/render-examples.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
